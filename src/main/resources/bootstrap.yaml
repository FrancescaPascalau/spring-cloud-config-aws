spring:
  application:
    name: demo
#  config:
#    import: "aws-parameterstore:"

aws:
  paramstore:
    fail-fast: true
    prefix: /config
    profileSeparator: _
    defaultContext: application
    region: eu-west-1
    enabled: true